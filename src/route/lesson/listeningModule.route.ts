// import express from 'express';
// //import { generateAudio, generateListeningFeedback } from '../controller/skill_Builder/listeningModule.controller';
// import { generateAndStoreModule } from '../../controller/builder.controller';

// const router = express.Router();
// router.post('/:type', generateAndStoreModule);
// //router.post('/feedback',generateListeningFeedback)

// export default router;